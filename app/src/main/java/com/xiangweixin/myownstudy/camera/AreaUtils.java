package com.xiangweixin.myownstudy.camera;

import android.graphics.Rect;

public class AreaUtils {

    /**
     * 转换对焦区域范围[-1000, -1000] [1000, 1000]
     * @param x
     * @param y
     * @param surfaceWidth
     * @param surfaceHeight
     * @param coefficient 区域比例大小
     * @return
     */
//    public static Rect calculateTapArea(float x, float y, int surfaceWidth, int surfaceHeight, float coefficient) {
//        float focusAreaSize = 200;
//
//        int areaSize = (int) (focusAreaSize * coefficient)
//
//        int centerX = (int) (x / surfaceHeight * 2000 - 1000);
//    }
//
//    private static int clamp(int x, int min, int max) {
//        if (x > max) {
//            return max;
//        }
//        if (x < min) {
//            return min;
//        }
//        return x;
//    }

}
